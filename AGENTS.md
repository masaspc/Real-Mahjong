# エージェント作業規約

このリポジトリは複数の AI エージェントが別々の worktree で並行実装する。
以下は衝突と手戻りを防ぐための取り決めである。

## 設計と計画

- 設計仕様: `docs/superpowers/specs/2026-08-08-real-mahjong-design.md`
- 実装計画: `docs/superpowers/plans/` 配下
- 仕様と食い違う実装が必要に見えたら、実装を進めずコーディネータへ報告する

## 触ってよい範囲

- 自分に割り当てられたクレートの、自分のファイルのみを編集する
- **`lib.rs` と `mod.rs` は編集しない。** モジュール木は Wave 0 で確定済みである。
  新しいファイルが必要になったらコーディネータへ報告する
- 他のクレートを編集しない。他クレートの API が足りない場合も、自分で足さずに報告する
- `crates/protocol` は凍結済みである。変更が必要に見えたら必ず報告する
- **`mahjong-core/src/hand.rs` と `shapes.rs` は共有語彙であり編集禁止。**
  判定系と点数系が同じ表現の上で書くための土台である

### mahjong-core のファイル所有権

| ファイル | 所有 |
|---|---|
| `hand.rs` / `shapes.rs` | Wave 0（編集禁止） |
| `shanten/*.rs` / `wait.rs` / `furiten.rs` / `callable.rs` | Wave 1a |
| `decompose.rs` / `yaku_check/*.rs` / `fu.rs` / `score.rs` | Wave 1b |

Wave 1a は面子分解が必要になっても `decompose.rs` を編集せず、自前の探索を
`shanten/standard.rs` の中に持つ。Wave 1b は待ち形の判定を再実装せず、
`wait.rs` の公開結果を消費する。

**ロンの可否はどちらにも実装しない。**役の有無に依存するため、Wave 2 で engine が結線する。

## 期待値テーブル

- `fixtures/` は仕様側の資産である
- **既存の期待値を変更してはならない。** 自分の実装が通らない場合、まず実装を疑う
- 期待値が誤っていると確信した場合は、根拠（採点の内訳）を添えて報告する
- ケースの追加は歓迎する。追加時は既存ファイルではなく新しい JSON ファイルを作る
  （同じファイルを複数エージェントが編集すると衝突するため）

## 完了条件

自分の作業単位は、次がすべて通って初めて完了である。

```bash
cargo test --workspace
cargo clippy --all-targets -- -D warnings
cargo fmt --check
```

「実装したが未検証」の状態で完了を報告しない。

## 進捗の報告

意味のある区切りで worktree のコメントを更新する。

```bash
orca worktree set --worktree active --comment "向聴数の標準形を実装。七対子形に着手" --json
```

更新すべき区切り: 着手、主要な部品の完成、テスト通過、行き詰まり、完了。

**このコメントはコーディネータが状態を知る唯一の手段である。**Orca の
worktree で動くエージェントは、完了も行き詰まりもコーディネータへ通知しない。
黙って止まっていても外からは分からないので、必ず更新する。

**書き方とステータスの付け方は「ワークツリーの状況報告」節に従う。**
コメントの文面と `--workspace-status` の使い分けはそちらが規定する。

### コーディネータ側の監視

worktree でエージェントを起動したら、**同時に監視を張る。**

```bash
tools/watch_worktree.sh <worktree名> <worktreeのパス>
```

コミット数・未コミットの件数・worktree のコメントを見て、変わったときだけ
1行出す。`Monitor` で常駐させると、完了と行き詰まりの両方が通知として届く。

**「終わったら見に行く」で済ませない。**Wave 2c と 2d で、完了と行き詰まりを
2回ともユーザーに指摘されて初めて気づいた。監視の仕組みを張らなかったのが
原因である。

## ワークツリーの状況報告

区切りのたびに Orca のワークツリー状態を更新すること。サイドバーのカードに表示され、
ユーザーはこれを見てプロジェクトを切り替えずに進捗を把握する。

```bash
orca worktree set --worktree active --comment "<現在地と次の一手>" --json
orca worktree set --worktree active --workspace-status <todo|in-progress|in-review|completed> --json
```

**更新するタイミング**

- 調査が終わり方針が決まったとき
- 実装の区切り（フェーズ完了、テスト通過）
- **ユーザーの判断が必要になったとき（最重要）**
- 詰まって進めなくなったとき

**コメントの書き方**

- 60〜100字程度。カードで読み切れる長さにする
- 「何をしたか」ではなく「**今どこにいて、次に何が起きるか**」
- ユーザーの判断待ちなら冒頭に `【要確認】` を付け、`--workspace-status in-review` も設定する
- 完了報告には検証の根拠を入れる（テスト件数、通過したチェック）
- 「順調」「作業中」だけの主観的な要約は書かない

**ステータスの意味**

| 値 | 意味 |
|---|---|
| `todo` | 着手前 |
| `in-progress` | 作業中 |
| `in-review` | ユーザーの確認待ちで停止中 |
| `completed` | 完了。マージ・アーカイブ可能 |

**例**

```
【要確認】設計書の改訂完了。レビュー機能削除とURL変更の承認待ち
Wave 3d完了。server 73件・clippy・fmt通過。実WSで打牌後14件まで確認済み
P2価格エンジン実装中。10タスク中6完了、残りは評価ロジック
```

失敗しても作業は止めない（ベストエフォート）。ただし `in-review` に入るときは必ず設定する。

## コミット

- テストが通った単位でコミットする
- コミットメッセージは日本語で、何をなぜ変えたかを書く
- 生成物（`apps/web/src/protocol/`）は手で編集せず、
  `cargo test --package protocol` で再生成する

## 計画を書く側の検算義務

これはコーディネータ向けの取り決めである。Wave 1 までに、計画へ書いたテストデータの
誤りが15件見つかった。すべて機械で検出できるものだった。**計画を書いたら、実装者へ
渡す前に次を機械で検査する。**目視で済ませない。

```bash
python3 tools/check_plan.py docs/superpowers/plans/<計画>.md
```

| 検査 | 内容 | 自動 |
|---|---|---|
| 牌の枚数 | 手牌は13枚（副露があれば `13 - 副露数×3`）。和了牌を加えて14枚 | あり |
| 記法の妥当性 | 字牌は 1z..7z のみ。数牌は 0..9（0は赤ドラ） | あり |
| 和了形かどうか | 和了すると書いたケースが標準形・七対子・国士のいずれかで実際に和了する | 手動 |
| 文書内の整合 | 冒頭の方針と後半のコード例が矛盾していないか | 手動 |
| 文字の混入 | 日本語・ASCII 以外の文字が紛れていないか | あり |
| テスト数の一致 | 掲載した `#[test]` の数と「N テスト PASS」が合っているか | あり |
| タスク境界の前方参照 | あるタスクのコードが、後のタスクで足す関数・フィールド・定数を使っていないか | あり |
| 読まれないフィールド | 宣言したタスクの中で一度も読まないフィールドが無いか（`-D warnings` が `dead_code` で落ちる） | あり |
| 関数の名前解決 | テストモジュール内の呼び出しが、その `use`・親の `use`・自分の定義から解決できるか | あり |
| 型の名前解決 | 戻り値と型注釈に書いた型が解決できるか | あり |

**下4つは Wave 2c と 2d のレビューで繰り返し見落とした類型である。**段階的に
組み立てる計画では、タスク N のコードがタスク N+1 で足すものを使っていても
最終形だけ見れば正しく見える。目視では捕まらない。

既存ファイルへ差分を当てる計画では、親モジュールの `use` が計画本文に
現れない。検査は `Modify:` / `Create:` に書かれたファイルを読んでスコープを
補うので、**対象ファイルのパスを正しく書くこと。**

`tools/check_plan.py` は、`### Task N:` の見出しが1つも無ければそこで止まる。
タスクを分けた検査がすべて空振りするためである。**何も検査しなかったときに
OK と言う検査器は、無いより悪い。**Wave 3c の計画は見出しを `## Task` と
書いていたため、前方参照の検出を含む3つの検査が黙って通っていた。

## 計画のコードは走らせてから渡す

**計画に載せるコードとテストは、書いたあと実際にコンパイルして実行する。**
静的な検査を全部通っても、実行しなければ分からない誤りが残る。

やり方は、既に凍結されたクレートを `path` 依存で参照する試作クレートを
scratchpad に作り、計画のコードブロックをそのまま貼って `cargo test` と
`cargo clippy --all-targets -- -D warnings` を回す。計画本文からコードブロックを
機械的に抜き出して配置すれば、計画と試作が食い違うこともない。

Wave 3c でこれをやったところ、静的検査と Codex レビューの両方を通り抜けた
誤りが2件見つかった。

| 見つかった誤り | 静的検査 | Codex | 実行 |
|---|---|---|---|
| 親の配牌を14枚と期待（実際は全席13枚、14枚目は `Draw`） | 素通り | 検出 | 検出 |
| `SeedSource::next` が `Iterator::next` と紛らわしく `-D warnings` で落ちる | 素通り | 素通り | 検出 |
| 席が留守のあいだ3秒待つテスト（親は人間なので25.75秒まで何も起きない） | 素通り | 素通り | 検出 |

**実測した値は計画に書く。**「およそ25秒」ではなく「25,800ms（式は
5,000 + 20,000 + 500 + lead_in 250 = 25,750、その次のポーリング）」と書く。
実装者が期待値の根拠を再構成できないと、落ちたときにテストのほうを
書き換えてしまう。

**「枚数は合っているが意味が違う」類型に注意する。**14枚の完成形を「和了牌を含まない
手牌」として書いた誤りが11件あった。枚数だけの検査では捕まらない。

### 着手前のレビューを通す

機械検査は数値しか見ない。「ルールとして必要な局面が揃っているか」「凍結済みの API で
足りるか」は人手（別のエージェント）でないと出てこない。実際、Wave 2a の初稿は機械検査を
すべて通過したうえで、レビューでブロッカーが9件出た。

> **実装計画は、Codex のレビューで**ブロッカーがゼロになるまで**書き直しを繰り返す。
> 指摘が残っている計画を実装者へ渡さない。**

各回で行うこと。

1. 計画を書く、または前回の指摘を反映する
2. 機械検査（上表）を通す
3. Codex にレビューさせる
4. 指摘を一件ずつ検算する。**正しければ直し、誤っていれば根拠を示して退ける**
5. ブロッカーが残っていれば 1 へ戻る

指摘を鵜呑みにしない。過去に「ポンと明槓の同順位競合」の指摘を、牌が1種4枚しかない
という数え上げで退けた例がある。ただし**退けるには必ず検算を示す**。

### レビュー依頼に前回の指摘を再掲しない

Wave 3c の第3ラウンドで、Codex が前回の回答をほぼ逐語的に再出力した。引用していた
行番号は既に差し替えた古い版のもので、テスト件数も古かった。原因はこちらにある。
プロンプトの4割を前回の指摘の再掲に費やしたため、Codex がそれに引きずられて
ファイルを読み直さずに同じ結論を再構成した。

依頼にはこう書く。

- **前回の指摘を書かない。**「対応済み」の一覧も書かない
- 「過去のレビュー内容は一切参照せず、いまディスクにあるファイルだけを読んで判断せよ」
- 現在の行数・テスト件数・タスク数を伝え、**「この数と食い違う前提で書き始めたら古い版を見ている」**と明示する
- **指摘には現在の行番号と実際の文字列の引用を義務づける。**引用が一致しない指摘は無効とする

引用を義務づけると、焼き直しかどうかが機械的に判定できる。第4ラウンドはこの形にして
初めて、現在のファイルを読んだ新しい指摘が2件返ってきた。

## 監視は「止まったこと」も検出する

エージェントを起動したら同時に監視を張る。**そのとき、状態の変化だけを
見てはならない。**Codex は "running" のまま静かに死ぬことがある。
Wave 3c の第2ラウンドのレビューは 6 時間 43 分そのままだったが、
状態変化だけを見ていた監視は一度も鳴らなかった。

```bash
tools/watch_codex.sh <job-id> [停滞とみなす秒数]      # Codex のジョブ
tools/watch_worktree.sh <名前> <パス>                  # Orca の worktree
```

`watch_codex.sh` はログの更新時刻を見て、伸びなくなったら鳴る。
**沈黙は成功ではない。**監視を張るときは「いまこれが落ちたら、この監視は
何か出力するか」を自問すること。答えが「出力しない」なら、その監視は
役に立たない。

## 麻雀ドメインの注意

- 向聴数の規約は **-1 が和了、0 がテンパイ**
- 牌の記法は `123m456p789s1234567z`、赤ドラは `0m` / `0p` / `0s`
- 字牌は 1z=東, 2z=南, 3z=西, 4z=北, 5z=白, 6z=發, 7z=中
- 牌のエンコードは 0..=36。0..=33 が通常牌、34=赤5m, 35=赤5p, 36=赤5s
- ルールは雀魂「金の間」準拠。判断に迷う点は `Ruleset` の値を見る。
  そこに無い挙動は仕様書を見る。どちらにも無ければ報告する

## サーバの真実をクライアントへ出さない

- `Event` はサーバの真実であり、**TypeScript 型を生成しない**
- クライアントへ出る形は `ClientEvent` のみで、`project()` を通して作る
- `mahjong-wasm` は `mahjong-core` にのみ依存する。
  **`mahjong-engine`（山を持つ側）を参照した瞬間、クライアントが山を復元できる**
