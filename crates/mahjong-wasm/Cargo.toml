[package]
name = "mahjong-wasm"
version = "0.1.0"
edition.workspace = true
license.workspace = true

# クライアントへは core の判定系のみを出す。
# mahjong-engine（山を持つ側）をここへ足してはならない。
[dependencies]
protocol = { path = "../protocol" }
mahjong-core = { path = "../mahjong-core" }

[lints]
workspace = true
