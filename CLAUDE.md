# Real Mahjong — エージェント向けの申し送り

作業規約は `AGENTS.md` にある。触ってよい範囲・完了条件・計画の検算義務は
そちらを読むこと。ここには Orca 上での状況報告だけを置く。

## ワークツリーの状況報告

区切りのたびに Orca のワークツリー状態を更新すること。サイドバーのカードに表示され、
ユーザーはこれを見てプロジェクトを切り替えずに進捗を把握する。

```bash
orca worktree set --worktree active --comment "<現在地と次の一手>" --json
orca worktree set --worktree active --workspace-status <todo|in-progress|in-review|completed> --json
```

**更新するタイミング**

- 調査が終わり方針が決まったとき
- 実装の区切り（フェーズ完了、テスト通過）
- **ユーザーの判断が必要になったとき（最重要）**
- 詰まって進めなくなったとき

**コメントの書き方**

- 60〜100字程度。カードで読み切れる長さにする
- 「何をしたか」ではなく「**今どこにいて、次に何が起きるか**」
- ユーザーの判断待ちなら冒頭に `【要確認】` を付け、`--workspace-status in-review` も設定する
- 完了報告には検証の根拠を入れる（テスト件数、通過したチェック）
- 「順調」「作業中」だけの主観的な要約は書かない

**ステータスの意味**

| 値 | 意味 |
|---|---|
| `todo` | 着手前 |
| `in-progress` | 作業中 |
| `in-review` | ユーザーの確認待ちで停止中 |
| `completed` | 完了。マージ・アーカイブ可能 |

**例**

```
【要確認】設計書の改訂完了。レビュー機能削除とURL変更の承認待ち
Wave 3d完了。server 73件・clippy・fmt通過。実WSで打牌後14件まで確認済み
P2価格エンジン実装中。10タスク中6完了、残りは評価ロジック
```

失敗しても作業は止めない（ベストエフォート）。ただし `in-review` に入るときは必ず設定する。

## ループで回せる計画書の書き方

管制塔（`~/orca/agent-coordinator`）の `loop.py` は、計画書からタスクを機械的に
切り出してワーカーへ渡す。人間はタスクごとの指示を出さない。
**指示の中身は計画書に前もって書いておく。**

自律の水準は L2 固定。**コミットまでは自動、マージと push は必ず人間が判断する。**

この規約を満たしていない計画書はループに乗らず、人間が手で回すことになる。

### 見出しの形

タスクは必ずこの形にする。`loop.py` はこの正規表現で切り出す。

```
### Task <番号>: <題名>
```

- 番号は 1 から連番。飛ばさない
- 全角コロン `：` も可
- `### Task 3: ...` のように深い見出しでもよい
- **1つのタスク＝1つのコミット**にする。ワーカーはタスク単位で起動され、終わると消える

### タスクに必ず入れるもの

ワーカーは**そのタスクの節しか読まない**前提で書く。前後の文脈は渡らない。

```markdown
### Task 8: 公開クエリヘルパ

（何のためのタスクか。1〜3行）

**Files:**
- Create: `db/queries.ts`
- Modify: `package.json`

**Interfaces:**
- Consumes: `db`（Task 6）
- Produces: `listPublishedGrades(filters): Promise<{ rows, total }>`

- [ ] **Step 1: ...**

（実装の中身。コードを載せてよい）

- [ ] **Step 6: テストが通ることを確認**

Run: `npm run test:integration`
Expected: PASS 6 tests

- [ ] **Step 7: コミット**

（git add の対象を明示指定した git commit。`git add -A` は使わない）
```

**外せない2つ**

1. **検証コマンドと期待値** —— `Run:` と `Expected:` を必ず書く。
   検証役（Checker）はこれを実行して合否を判定する。これが無いとループが合否を決められない
2. **触ってよいファイルの明示** —— `Files:` に無いファイルは触らせない。
   並列で別のワーカーが動いていても衝突しないため

### ループが自動で足すもの

以下は `loop.py` が毎回先頭に注入するので、計画書に書かなくてよい。

```
- コミットまでは行ってよい。マージと push は絶対に行わない（人間が判断する）
- 指定されたタスクの範囲だけを触る。次のタスクに手を出さない
- 判断に迷ったら勝手に決めず、worker_done の body に明記して終える
- テストを通すために既存テストの期待値を緩めない
```

### 検証は別のエージェントがやる

Maker が `codex` なら Checker は `claude`、逆も同じ。**同じエージェントには絶対にしない。**
自分が書いたコードを自分で採点すると甘くなるからである。

Checker には「コードは書くな。検証のみ」と指示が飛ぶ。

### ループに乗せてはいけないもの

3つの条件を満たさないタスクは、ループではなく人間が回す。

| 条件 | 意味 |
|---|---|
| 反復性 | 同じ手順が繰り返し現れる |
| 機械的な検証可能性 | テストやビルドで合否が決まる。主観の入る余地がない |
| 経済的な価値 | ループ税（トークン消費が5〜30倍）に見合う |

**設計判断・仕様の選択・UI の見た目の評価はループに乗せない。**
これらは `【要確認】` を付けて人間に上げる。

### ループが止まる条件

止まると管制塔がワークツリーを `in-review` にし、コメント冒頭に `【要確認】` を置く。
`./status.sh` と Orca のサイドバー、スマホ通知に出る。

- 指定タスク数を終えた（正常終了。マージ判断を待つ）
- 検証が規定回数通らない（サーキットブレーカー）
- コミットが進まない周回が続く（停滞検出）
- 実時間の上限
- ワーカーが判断を求めてきた（`ask` / `escalation`）

### 投入の条件

管制塔は次の3つが揃ったワークツリーにだけループを投げる。

1. エージェントが動いていない
2. 未コミットがゼロ
3. 計画書に残タスクがある

**作業を終えたらコミットして手を離す。** 未コミットを残したまま止まると、
ループが投入できず順番待ちになる。
